-# = render "shared/header"
= render "shared/sideber"
- @page_title = "会員名簿"
%h1= @page_title
.toolbar
  = link_to "会員の新規登録", :new_member
.members_index
  - if @members.present?
    - @members.each do |member|
      .members_index_box
        = link_to member.user_name, member
        = link_to "編集", [:edit, member]
        |
        = link_to "削除", member, method: :delete, data: { confirm: "本当に削除しますか?" }
  - else
    %p 会員情報がありません。